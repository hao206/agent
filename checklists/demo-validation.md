# Demo Validation Checklist

- Backend compile passes (`python -m compileall src tests`).
- Frontend lint/build passes.
- `/api/v1/health` responds in local backend smoke test.
- Demo shows plan confirmation (HITL gate) and resume.
- Demo includes one construction request and one out-of-scope request.
- README commands match current package layout.
