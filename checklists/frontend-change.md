# Frontend Change Checklist

- Read `.agents/skills/frontend/SKILL.md`.
- Preserve streaming chat behavior.
- Keep `session_id` propagation intact.
- Keep chat input disabled during streaming or HITL interrupt.
- Render structured construction payloads as UI components when available.
- Run `npm run lint` and `npm run build` from `frontend/`.
